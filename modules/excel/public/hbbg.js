import { fbData } from "../var/index";

// 合并表格
export function hbbg() {
}
// 对比合并表格
export function dbhbbg() {
  fbData = fbData.filter(it => {
    const a = 'c0', { [a]: c0 } = it
    console.log(c0)
  })
}